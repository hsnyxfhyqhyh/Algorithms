Insert sort: 

starts from index 1 (k), make sure the first k+1 elements are in order, from smallest to bigger in each every loop. 
	store the current index item to cur; 
	compare each every item in front of the current item is smaller than cur; if not shift it to the next. 
	set cur to the empty spot.    

  